name: Frontend CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'ecommerce-frontend/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'
        cache-dependency-path: ecommerce-frontend/package-lock.json

    - name: Install dependencies and Build
      env:
        STRIPE_PUB_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
      run: |
        cd ecommerce-frontend
        echo "VITE_API_BASE_URL=/api" > .env
        echo "VITE_STRIPE_PUBLISHABLE_KEY=$STRIPE_PUB_KEY" >> .env
        npm install
        npm run build

    - name: Deploy to EC2
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ubuntu
        key: ${{ secrets.EC2_SSH_KEY }}
        source: "ecommerce-frontend/dist/*"
        target: "/var/www/payreconcile/"
        strip_components: 2
